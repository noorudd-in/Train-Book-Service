const {
  validateCreateBooking,
  validateUpdateBooking,
  validateCreatePassenger,
} = require("./validateRequest");

module.exports = {
    validateCreateBooking,
    validateUpdateBooking,
    validateCreatePassenger
}